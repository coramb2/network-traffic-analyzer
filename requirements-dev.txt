# Test/lint dependencies. Install alongside both runtime requirement files:
#   pip install -r requirements.txt -r requirements-dashboard.txt -r requirements-dev.txt
pytest==9.0.3
ruff==0.15.21
# Browser-driven dashboard tests (tests/test_dashboard_browser.py) - also
# needs `playwright install --with-deps chromium` (see CI workflow).
pytest-playwright==0.8.0
# Supply-chain scanning gate (check_pip_audit.py) - see CI workflow.
pip-audit==2.10.1
